import sequtils,strutils
var
    c1:seq[int] = readLine(stdin).split(" ").map(parseInt)
    c2:seq[int] = readLine(stdin).split(" ").map(parseInt)
    c3:seq[int] = readLine(stdin).split(" ").map(parseInt)
    i,j,k,l,m,n:int
    a:array[3,int] = [i,j,k]
    b:array[3,int] = [l,m,n]
if i + l == c1[0]:
    if i + m == c1[1]:
        if i + n == c1[2]:
if i == 
