program calc;

var a: real;
    b: real;
    operater: char;

begin
  read(a, operater, b);
  writeln(a);
  writeln(b);
  writeln(operater);

  (* case operater of *)
  (*   '+': writeln(a + b); *)
  (*   '-': writeln(a - b); *)
  (*   '*': writeln(a * b); *)
  (*   '/': writeln(a / b); *)
  (* end; *)

end.
