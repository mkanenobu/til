{
  "name": "deno-cli-template",
  "version": "0.1.0",
  "scripts": {
    "exec": "deno run src/index.ts",
    "build": "deno compile --output a.out src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.5.0",
    "typescript": "^4.5.2"
  }
}
