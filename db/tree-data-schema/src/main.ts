import { withParentId } from "./with-parent-id";
import { withPath } from "./with-path";
import { closureTable } from "./closure-table";

const main = async () => {
  // await withParentId();
  // await withPath();
  await closureTable();
};

main();
